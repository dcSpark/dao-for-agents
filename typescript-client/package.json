{
  "name": "@dao-for-agents/typescript-client",
  "version": "1.0.0",
  "description": "TypeScript client library for interacting with the DAO smart contract",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["dao", "ethereum", "smart-contract", "typescript"],
  "author": "dcSpark",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^22.10.2",
    "ethers": "^5.7.2",
    "typescript": "^5.7.2"
  }
}
